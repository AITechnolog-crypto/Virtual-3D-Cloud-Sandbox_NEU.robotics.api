package com.june.repository;

import com.june.model.SchutzAnzug;
import org.springframework.data.mongodb.repository.MongoRepository;
import org.springframework.stereotype.Repository;

/**
 * Platzhalter für SchutzAnzugRepository
 */
@Repository
public interface SchutzAnzugRepository extends MongoRepository<SchutzAnzug, String> {
}
